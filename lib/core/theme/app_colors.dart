/*import 'package:flutter/material.dart';

class AppColors {
  // --- Couleurs Principales (Brand) ---
  static const Color primary = Color(0xFFFF7900); // Orange Car225
  static const Color primaryLight = Color(0xFFFFE0B2); // Orange très clair (pour les fonds)

  static const Color secondary = Color(0xFF00C853); // Vert
  static const Color secondaryLight = Color(0xFFC8E6C9); // Vert très clair

  // --- Couleurs Neutres ---
  static const Color background = Color(0xFFF5F5F5); // Gris très clair pour le fond des scaffold
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color greyDark = Color(0xFF424242);
  static const Color greyLight = Color(0xFFEEEEEE);
}*/



import 'package:flutter/material.dart';


class AppColors {

  // --- Couleurs Principales (Brand) ---
  static const Color primary = Color(0xFFFF7900); // Orange Car225
  static const Color primaryLight = Color(0xFFFFE0B2); // Orange très clair

  static const Color secondary = Color(0xFF00C853); // Vert
  static const Color secondaryLight = Color(0xFFC8E6C9);

  // --- Couleurs Neutres ---
  static const Color background = Color(0xFFF5F5F5); // Fond clair
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color greyDark = Color(0xFF424242);
  static const Color greyLight = Color(0xFFEEEEEE);

  // --- Couleurs Mode Sombre (Nouveau) ---
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkInputFill = Color(0xFF2C2C2C);
  
}